# قالب البرومبت — مشاريع التصاميم التفاعلية

> الصق هذا في بداية أي مشروع جديد (دعوة، بطاقة، بانر، صفحة مناسبة).
> عام لكل المشاريع — لا يخص مشروعًا بعينه.

---

## البرومبت (انسخيه كما هو)

```
اشتغل على هذا المشروع بالقواعد التالية، والتزم بها في كل خطوة:

■ طريقة الاستوديو — إلزامية لكل شيء
كل عنصر بصري في المشروع يُولَّد بـ ChatGPT، بلا استثناء:
الأيقونات، الزخارف، الزوايا، الفواصل، الأزرار، الأختام، الكونفيتي،
الإطارات، الشارات، الخلفيات. ممنوع ترسم بديلًا بالكود (SVG يدوي أو
أشكال PIL) وتعتبره حلًّا — لو النتيجة ما عجبتني أعِد التوليد ببرومبت
أفضل، لا تتخلَّ عن الطريقة.

قبل أي عمل: تأكد أن ChatGPT مفتوح في المتصفح.
إن لم يكن مفتوحًا — ذكّرني فورًا بفتحه قبل أن تكمل، ولا تبدأ بدونه.

■ اختبار الثيمات — قبل بناء أي شيء (إلزامي)
لا تبدأ التنفيذ بثيم واحد افترضتَه. أول خطوة دائمًا:
ولّد في ChatGPT عيّنة لكل ثيم مطروح أو خطر ببالنا — في صورة واحدة
لكل ثيم، تحوي نفس العناصر (كرة/رمز الحدث + زاوية + فاصل) حتى
تكون المقارنة عادلة.
ثم اعرضها كلها عليّ جنب بعض وانتظر اختياري قبل أن تكتب سطرًا واحدًا.

سبب هذه القاعدة: بناء ثيم كامل ثم اكتشاف أنه غير مناسب يضيّع
وقتًا طويلًا، والوصف بالكلام لا يكفي — لا بد أن أرى.

اقترح أنت ثيمات إضافية لم أذكرها إن رأيتها مناسبة، ولا تكتفِ
بالمذكور. وسمِّ كل ثيم باسم واضح (مثال: ريترو سبعينات، فينيل
ستينات، آرت ديكو، ريترو كوكيت، مودرن مينيمال).

■ الأسلوب البصري — الوسط المطلوب
لا فوتوريالستي (صور ثلاثية الأبعاد لامعة) ولا طفولي (ستيكرز منفوخة،
كرتون، حدود بيضاء سميكة، ألوان حلوى).
المطلوب بينهما: رسم خطّي أنيق / نقش محفور / إليستريشن راقٍ.
اذكر الأسلوب صراحةً في كل برومبت توليد، مع نفي الاتجاهين:
"NO photorealism, NO 3D, NO puffy stickers, NO cartoon,
 NO thick white outlines, NO bright candy colors"

■ ترتيب التوليد — الأساسيات ثم الزينة
**لا تبدأ هذه المرحلة إلا بعد موافقتي على ثيم من اختبار الثيمات.**
ثم ولّد على مرحلتين، ولا تخلط بينهما:
  ١) الأساسيات أولًا: الأيقونات، الرمز الرئيسي (الختم/الكرة)،
     الزوايا، الفواصل — كل ما لا تقوم الصفحة بدونه.
  ٢) بعد اكتمالها: عناصر التزيين التي تخدم المشروع — الشرائط،
     الإطارات، الحواف، الشارات، الأشرطة، الورود، الجسيمات.
السبب: الأساسيات تحدّد اللغة البصرية؛ لو ولّدت الزينة قبلها
ثم تغيّر الأساس، ترمي الزينة كلها وتعيدها.

■ قائمة العناصر — ولّدها كلها بالاسم (بعد اعتماد الثيم)
ولّد كل ما يلي بلا أن أطلبه واحدًا واحدًا، مجموعة كل ٥–٦ في صورة:

  الأساسيات:
   · الرمز الرئيسي للحدث (كرة ديسكو / رمز المناسبة)
   · زخرفة زاوية البطاقة
   · فاصل أفقي
   · أيقونات البرنامج (واحدة لكل فقرة)

  الإطارات والحاويات:
   · إطار بيضاوي لاسم صاحب المناسبة
   · إطار زر (فارغ من الداخل)
   · إطار مربع/مستطيل لصندوق المعلومات
   · شريط بانر منحنٍ
   · تذكرة مثقّبة
   · راية معلّقة + تاق بخيط

  أيقونات المعلومات (لكل تسمية في صندوق الموقع):
   · تقويم (التاريخ) · دبوس موقع (العنوان) · مبنى (المكان)
   · ساعة (الوقت/العدّاد) · سهم مشاركة · ظرف

  الهوية:
   · مونوجرام الحرف الأول
   · ختم شمعي
   · قوس/توبر لأعلى الترويسة

  الحركة والتفاصيل:
   · قطع كونفيتي صغيرة (١٤–١٦ قطعة، تُقرأ بحجم ٢٠px)
   · جسيمات متساقطة
   · باقة/زهور للختام
   · حافة جانبية طويلة

  الأصول الخارجية:
   · صورة معاينة المشاركة ١٢٠٠×٦٣٠ (og:image)
   · أيقونة الموقع favicon
   · خلفية أو نقشة متكررة (seamless pattern)

■ ما ينقص لاحقًا — سجّله ولا تتجاهله
بعد التركيب، افحص كل قسم في الصفحة وقارنه بالمكتبة.
أي قسم بلا عنصر بصري (تسمية عارية، صندوق بلا إطار، خلفية مسطّحة)
سجّله في قائمة النواقص وأخبرني به، ثم ولّده.
هذا الفحص إلزامي قبل اعتبار المكتبة مكتملة.

■ إعادة التوليد — لا تقبل عنصرًا ناشزًا
التوليد ليس مرة واحدة. أعِد توليد أي عنصر مرارًا حتى يضبط، في هذه الحالات:
  · **عدم تجانس**: سماكة خط مختلفة، لمعان أعلى، حجم أكبر،
    لون خارج اللوحة، أو أسلوب لا يطابق بقية المجموعة.
  · **زيادة أو نقص**: العنصر مزدحم بالتفاصيل فيحتاج تبسيطًا،
    أو فارغ أكثر من اللازم فيحتاج إثراءً.
  · **لا يخدم موضعه**: لا يُقرأ بحجمه الفعلي في الصفحة،
    أو يصطدم بالنص، أو يضيع على الخلفية.

كيف تعيد: عدّل سطر STYLE أو أضف نفيًا صريحًا لما ظهر خطأً
("NO thick lines", "simpler", "fewer details", "larger center space")،
ثم ولّد مجموعة كاملة من جديد — لا تولّد عنصرًا واحدًا منفصلًا
لأن ذلك يكسر التجانس مع بقية المجموعة.

قارن دائمًا الناتج الجديد بالقديم جنبًا إلى جنب قبل الاعتماد،
وأخبرني بما تغيّر ولماذا. تكرار ٣–٤ مرات لعنصر واحد أمر طبيعي
ومقبول — الأسوأ منه قبول عنصر ناشز لأنه "جاهز".

■ قواعد التوليد
· ولّد كل مجموعة في صورة واحدة — التوليد المنفصل يكسر التجانس
  (اختلاف سماكة الخط واللمعان بين العناصر).
· اذكر أكواد الألوان الست عشرية صراحةً، وقل "ONLY".
· اطلب مسافات واسعة بين العناصر وعدم تلامسها (ليسهل القص).
· اطلب خلفية بيضاء نقية أو سوداء نقية (حسب لون العنصر) للقص.
· "No text, no letters, no numbers" — دائمًا.
· لو النتيجة خارج الأسلوب: عدّل البرومبت وأعد، لا تصلحها بالكود.

■ نظام التصميم
· ٣–٥ ألوان كحد أقصى. لكل لون وظيفة واحدة ثابتة لا تتغير.
· تباين نغمي إلزامي: الخلفية أغمق بوضوح من البطاقة (أو العكس)،
  لا تجعل كل شيء بنفس درجة السطوع.
· الخطوط: خط للأسماء، خط للعناوين، خط للمتن، خط لاتيني.
  اختر خطوطًا جمالية لا افتراضية.
· انحناءات وتباعدات موحّدة عبر الصفحة كلها.
· توقيتات الحركة عائلة متناغمة لا أرقام عشوائية.

■ ممنوعات تصميمية (أخطاء متكررة)
· ممنوع letter-spacing على النص العربي — يكسر اتصال الحروف.
· ممنوع تدرّج لوني يستبدل لون النص العربي — يقطّع الحروف.
  (لمعة فوق الحروف مقصوصة بـ background-clip:text مقبولة،
   بشرط بقاء النص الأساسي مصمتًا تحتها.)
· ممنوع وضع أصل مُولَّد على خلفية بلون قريب من ألوانه —
  افحص لون خلفية الأصل الأصلية أولًا، وإلا تختفي تفاصيله
  وتضطر لتبييضه فيبهت.
· ممنوع تكرار معرّفات (id) في SVG داخل الصفحة —
  إخفاء نسخة يُفقد الأخرى تدرّجاتها.
· ممنوع الاعتماد على روابط خطوط خارجية — ضمّن الخطوط base64
  (الاستضافة قد تحجب الطلبات الخارجية فتسقط لخطوط النظام).
· عند تغيير أي أصل: أعد ضبط أحجام العناصر المعتمدة عليه
  (الكونفيتي والجسيمات تحديدًا) — الحجم القديم غالبًا يصير خاطئًا.

■ التحقق — إلزامي قبل أي تسليم
· افتح الصفحة في متصفح حقيقي مع حجب كامل للشبكة، وتأكد أنها تعمل.
· قِس ولا تخمّن: إحداثيات العناصر، التصادمات، شدة التأثيرات،
  تباين النصوص — بالأرقام من المتصفح لا بالنظر.
· صفر أخطاء JavaScript.
· احترم prefers-reduced-motion: كل الحركة تتوقف والمحتوى يظهر كاملًا.
· قبل اعتبار العمل منتهيًا: راجع قائمة الميزات الموثّقة سابقًا
  وتأكد أن إعادة البناء لم تُسقِط أيًّا منها.

■ التواصل
· ردّ بالعامية الخليجية.
· إن أخطأت، قل الخطأ صراحةً وسببه بلا تجميل.
· إن رأيت مشكلة في طلبي، قلها مباشرة واقترح البديل.
```

---

## اختبار الثيمات — الصيغة الجاهزة

كرّري هذا البرومبت مرة لكل ثيم، مع تغيير سطر `STYLE` و`Colors` فقط.
بقاء بقية النص ثابتًا هو ما يجعل المقارنة عادلة:

```
Create ONE image on pure white with these three elements, widely
separated and not touching: (1) a hanging disco ball, (2) a card
corner ornament, (3) a slim horizontal divider.

STYLE: [اسم الثيم + وصفه بدقة]
Colors: [HEX] [HEX] [HEX] ONLY, on white.

NO photorealism, NO 3D render, NO puffy stickers, NO cartoon.
No text, no letters, no numbers.
```

**أمثلة لسطر STYLE:**

| الثيم | سطر STYLE |
|---|---|
| ريترو سبعينات | `1970s retro poster illustration, bold simple shapes, flat screen-print look, warm nostalgic groovy vibe, thick confident lines` |
| فينيل ستينات | `1960s vinyl record sleeve design, clean mid-century modern shapes, calm and refined` |
| آرت ديكو | `1920s art deco fine-line engraving, delicate thin metallic linework, geometric and symmetrical` |
| ريترو كوكيت | `retro coquette scrapbook style, ribbons and bows, striped patterns, playful yet delicate` |
| مودرن مينيمال | `minimal modern line art, very few strokes, generous negative space, calm and clean` |

---

## برومبتات التوليد — صيغة جاهزة

**مجموعة عناصر زخرفية:**
```
Create ONE image on pure white with these elements, widely separated
and not touching: (1) …, (2) …, (3) ….
STYLE: [الأسلوب] fine-line illustration, delicate thin linework,
geometric and symmetrical, luxury aesthetic.
Colors: [HEX] and [HEX] ONLY, on white.
NO photorealism, NO 3D, NO puffy stickers, NO cartoon,
NO thick white outlines, NO bright candy colors.
No text, no letters, no numbers.
```

**مجموعة أيقونات موحّدة:**
```
A set of 5 small round icon medallions, widely separated on pure white,
each inside its own thin circular frame: (1) … (2) … (3) … (4) … (5) ….
All five must be identical in line weight and circle size.
SAME STYLE and colors as before. [نفس النفي أعلاه]
```

**إطار زر:**
```
A rectangular pill-shaped button frame with thin corner flourishes,
empty inside (for text later). SAME STYLE and colors. [نفس النفي]
```

**قطع كونفيتي:**
```
A set of 14 small separate confetti pieces, spread far apart on pure
white with clear empty space between each: diamonds, faceted lozenges,
thin tapered shards, four-pointed sparkles, small circles, slim
rectangles. SAME STYLE and colors. [نفس النفي]
```

---

## ملاحظة للاستخدام

بعد التوليد، القص والتركيب يتمّان برمجيًا (تفريغ الخلفية بالفيضان من
الحواف، فصل المكوّنات المتصلة، دمج الحد الأبيض مع الحشوة، ثم التضمين
base64). هذه خطوة تقنية لا تصميمية — التصميم كله يبقى من التوليد.

---

# قواعد متراكمة — من أخطاءٍ وقعت فعلًا

> كل قاعدة هنا ثمنُ خطأ. لا تُحذف واحدةٌ منها لأنها تبدو بدهية —
> بداهتُها اليوم من كونها كُتبت، لا من كونها كانت معروفة.
> أُضيفت بعد دعوة سند ونورة (٢٠٢٦).

## أولًا · التوليد

**١ · اذكر درجة الواقعية صراحةً، ولا تكتفِ بالنفي.**
`NO photorealism, NO cartoon` يقول للمولّد أين لا يقف، ولا يقول أين
يقف. فيخمّن، والتخمين هو سبب اختلاف السماكة واللمعان بين صورةٍ
وأخرى في المجموعة الواحدة. أضف سطر `REALISM` بسلّمٍ من عشرة:

| الدرجة | الوصف |
|---|---|
| ٠ | تجريد محض، لون بلا شكل |
| ٢ | رمزي هندسي — مُخترَع لا مرسوم عن شيء |
| ٤ | نقش خطّي علمي — نِسَبٌ صحيحة، بلا ظلٍّ ولا حجم |
| ٦ | إليستريشن بحجم — ظلٌّ خفيف، لا يزال مرسومًا |
| ٨ | شبه فوتوغرافي — ملمسٌ وعمقُ ميدان |
| ١٠ | فوتوريالستي |

والرقم وحده لا يكفي: المولّد لا يملك مسطرةً يقيس عليها. اتبعه
بجملتين — ما الذي يُرسم بدقّة (النِّسَب، عدد البتلات، التعريق)، وما
الذي يُمنع (`NO shading, NO volume, NO cast shadow, NO highlight`).
النفي المفصَّل هو ما يشتغل، لا الرقم المجرَّد.

**٢ · وحّد درجة الواقعية عبر المشروع كلّه.**
عنصرٌ عند ٦ بجانب عنصرٍ عند ٤ يجعل الأوّل يبدو مقصوصًا ملصوقًا
والثاني مسطّحًا باهتًا. اختر الدرجة مرّةً في اختبار الثيمات والتزمها.
والاستثناء الوحيد المقبول: **الخامة** (نسيج ورق، حبيبات) — واقعيّتها
في المادّة لا في مرسوم، فلا شيء فيها يزاحم العناصر في مستواها.

**٣ · برومبت الخلفية غير برومبت العنصر.**
العنصر يُولَّد على أبيض نقيّ لأنه سيُقصّ. والخلفية لا تُقصّ، فتُولَّد
بلون الورق نفسه أرضيةً مالئةً للإطار (`ground color #XXXXXX filling
the entire canvas`)، وإلا تفريغُها يأكل تدرّجاتها.

**٤ · احجز الفراغ الذي سيشغله النصّ، في البرومبت لا بعده.**
قل للمولّد أين يقف: «النشاط عند الحواف، والوسط يبقى فارغًا لونًا
مسطّحًا». إصلاحُ خلفيةٍ ازدحم وسطُها بعد التوليد مستحيل — تُعاد.

**٥ · عند إعادة التوليد: عدّل الرقم وأضف نفيًا لما ظهر، لا تُعِد الصياغة.**
`NO shading` أو `flatter` أو `thinner uniform lines` أسرع وأدقّ من
برومبتٍ جديد، لأن الجديد يغيّر أشياء لم تكن معطوبة.

## ثانيًا · قياسٌ لا تخمين

**٦ · كلّ حكمٍ بصريّ يُقاس بالأرقام من المتصفّح.**
التباين، والإحداثيات، والتصادمات، والإطارات في الثانية، وذروةُ
منحنى الحركة، وفرقُ البكسل. «يبدو جيّدًا» ليس نتيجة.

**٧ · قِس بجولاتٍ متداخلة، لا بجولةٍ واحدة.**
قياسُ الأداء مرّةً لكلّ نسخةٍ يقيس حرارةَ الجهاز لا النسخة. أدِر
أ-ب-أ-ب ثلاث مرّات، بخنقِ معالجٍ صريح، وخذ الوسيط.

**٨ · اختبار الضبط لا يكون مضبوطًا إلا إذا عُزل فعلًا.**
حين قارنتُ نسخةً «كاملة» بنسخةٍ مقسومة، كان `clip-path` لا يزال
مطبَّقًا على الضابطة فأفسدها. افحص أن الضابطة ضابطة.

**٩ · بعد أي إعادة بناء: قارن بالبكسل بما قبلها.**
طُلب منّي فتحٌ أفقيّ فتغيّر شكلُ الغلاف دون أن أنتبه — موضعُ الشعار
وحجمُ البيانات وميلُ الظلّ. المقارنةُ بالبكسل تكشف ما لا تكشفه العين،
والهدفُ رقمٌ صريح (بلغتُ ٩٨٫٧٪ تطابقًا).

**١٠ · راجع قائمة الميزات قبل التسليم.**
إعادةُ البناء تُسقِط ميزةً بصمت، والصمتُ هو المشكلة.

## ثالثًا · الحركة والأداء

**١١ · مدّةُ إتمام السحب تُشتقّ من المسافة الباقية.**
مدّةٌ ثابتة تعني أن من سحب ٩٠٪ يرى الباقي يقفز. الصيغة:
`المدة = المدة_الكاملة × المسافة_الباقية`، فتثبت السرعة مهما سحب.

**١٢ · ذروةُ منحنى الحركة تُحسب لا تُقدَّر.**
`cubic-bezier(.28,.02,.12,1)` ذروتُه ٢٫٩٧× متوسّطَ سرعته — يُقرأ
انقضاضًا لا انسيابًا. حسبتُ ميلَ المنحنى فاخترتُ `(.35,.35,.75,1)`
وذروتُه ١٫٢٣.

**١٣ · لا تُخفِ عنصرًا متحرّكًا إلا بعد انتهاء حركته.**
`.revealed #cover { visibility: hidden }` أطلقت بعد ٤٠م.ث من بدء
فتحٍ مدّتُه ٢٤٠٠، فبُترت الحركة. الشكوى وصلتني «سرعةً قاتلة» وهي
بترٌ لا سرعة. اربط الإخفاء بمؤقّتٍ مساوٍ لمدّة الحركة.

**١٤ · التقلّب ثلاثيّ الأبعاد أثقلُ ما في مشاهد الجسيمات.**
`perspective` + `rotateY` هو المهيمن على الكلفة. بديلُه انضغاطٌ
مستوٍ `scaleX` بين ١ و٫١٢ — الإحساسُ نفسه بربع الكلفة، فتُشترى به
جسيماتٌ أكثر.

**١٥ · ارسم الأشكال الرخوة مرّةً في سبرايت.**
الذرّات الضبابيّة تُرسم في كانفس صغيرةٍ مرّةً ثم `drawImage`، والكانفس
عند dpr = ١ لأن الضبابيّ لا يكشف الدقّة. رفع هذا الإطارات من ٢٤٫٨
إلى ٤٣٫٨.

**١٦ · احترم `prefers-reduced-motion`: تقف الحركة ويظهر المحتوى كاملًا.**
لا ناقصًا ولا شفّافًا.

## رابعًا · مزالق CSS وSVG

**١٧ · `fill-rule="evenodd"` يعمل داخل المسار الواحد لا عبر المسارات.**
الثقب يكون مسارًا فرعيًّا في نفس `<path>`، وإلا امتلأ.

**١٨ · لا تكرّر معرّفات SVG داخل الصفحة.**
نسختان من الشعار بنفس `id` للتدرّج: إخفاءُ إحداهما يُفقد الأخرى
تدرّجَها. ألحِق لاحقةً بكلّ نسخةٍ عند الحقن.

**١٩ · متغيّرٌ مخصَّصٌ داخل اختصار `transition` يُبطل التصريح كلّه.**
`var()` غيرُ صالحةٍ عند التحليل تجعل المدّة صفرًا، فتقفز الحركة بلا
أن يظهر خطأ. اكتب المدّة قيمةً صريحةً، أو ضعها في النمط السطريّ.

**٢٠ · المدّة السطريّة تغلب صحيفةَ الأنماط.**
`transition-duration` سطريّة تُبقي انتقالًا أثناء السحب رغم
`.dragging { transition: none }`. امسحها عند `pointerdown`.

**٢١ · توقيتُ الانتقال يُقرأ من الحالة بعد التغيير.**
لإخفاءِ شيءٍ فورًا تضع `transition: none` على **حالة الإخفاء** لا على
الحالة الظاهرة. وضعتُها في الموضع الخطأ فبقي التلميح يتلاشى ٠٫٧ث.

**٢٢ · خاصّية `translate` مستقلّةٌ عن `transform`.**
فيتعايش انزياحُ التمرير مع حركةٍ تستعمل `transform`، بلا أن يدهس
أحدُهما الآخر.

**٢٣ · طبقتان مركَّبتان مختلفتا الحجم أو الموضع تُرسَّمان على طورٍ
مختلفٍ من البكسل عند تكبيرٍ كسريّ، فيظهر خيطٌ بينهما.**
لا يعالجه تقريبُ البكسل ولا تراكبٌ بجزءٍ من بكسل. العلاج: اجعل
الطبقتين صندوقين **متطابقين** بعرضٍ كامل، ثم اقتطع كلًّا منهما
بـ`clip-path` — فتتّحد المصادر وينتفي الطور.

**٢٤ · أعمدةٌ متساوية بالشبكة تُغني عن قياسٍ بالجافاسكربت.**
`minmax(0,1fr) auto minmax(0,1fr)` مع `width: max-content` توازن
اسمين حول فاصل. القياسُ بالجافاسكربت يتأخّر عن الخطّ ويرتجف.

**٢٥ · لا تضع أصلًا مولَّدًا على خلفيةٍ تقارب لونَه.**
افحص لون خلفيته الأصليّة أوّلًا، وإلا اضطررتَ لتبييضه فيبهت.

**٢٦ · حشوةُ الحاوية تتجاوز أيَّ شريطٍ زخرفيّ عند حافّتها.**
ملاحظةٌ في الأسفل بحشوة ٢٧px تحت شريط توهّجٍ عرضُه ٧٨px تختفي.

**٢٧ · ابدأ بـ`<!doctype html>`.** بدونه المتصفّح في وضع التوافق.

## خامسًا · الحالة والوصول

**٢٨ · خزّن كلّ مؤقّتٍ وامسحه عند إعادة الضبط.**
مؤقّتُ نبضةٍ نجا من إعادة التشغيل فأطلق حركةً في مشهدٍ لا يخصّه.

**٢٩ · عنصرٌ قابلٌ للتركيز داخل منطقةٍ مخفيّة يبقى محطّةَ تنقّل.**
`aria-hidden` و`opacity: 0` لا يمنعان التاب. استعمل `inert` مع
`visibility: hidden`.

**٣٠ · تفضيلٌ اختاره المستخدم لا يُدهس بقيمةٍ افتراضيّة عند أيّ حدث.**
كتمَ الصوتَ قبل الفتح، ففتحَ فأعاد الفتحُ الصوت. احفظ الاختيار في
راية واحترمها.

**٣١ · النسخةُ بلا جافاسكربت تعرض نصَّ الدعوة كاملًا.**
كانت تعرض صياغةَ ما بعد المناسبة والمكانُ مخفيّ. من يفتحها بلا
جافاسكربت يرى دعوةً خاطئة، لا دعوةً ناقصة.

**٣٢ · ضمّن الخطوط base64.** الاستضافة قد تحجب الطلبات الخارجية.

**٣٣ · لا تغيّر مفتاح عدّادٍ بعد التوزيع.** العدد يعود صفرًا.

## سادسًا · في العمل نفسه

**٣٤ · أصلح العطب المطلوب ولا تحذف ما حوله.**
طُلب مني علاجُ حركةِ اسمٍ فحذفتُ المشهد كلَّه. الحذفُ ليس علاجًا،
وليس من حقّي أن أضيّق النطاق نيابةً عنها.

**٣٥ · قل الخطأ بسببه، ولا تلوِ عنق الحقيقة.**
قلتُ إن العدّاد يحتاج خادمًا، فأرتني دعوةً فيها عدّادٌ بلا خادم.
كان تأطيري ضيّقًا؛ قلتُها صراحةً ونفّذتُ.

**٣٦ · اذكر الناقص صراحةً عند التسليم.**
ما لم يُرفع، وما لم يُختبَر على جهازٍ حقيقيّ. التسليمُ الصامتُ عن
نقصٍ تعرفه غشّ.

**٣٧ · سكربت التعديل الذي يتوقّف في منتصفه يترك الملفّ غير مكتوب.**
تغيّر نصُّ المرساة فأخفق البحث. تحقّق من إصابة كلّ مرساةٍ قبل الكتابة،
وأعِد قراءة الملفّ إن أخفقت.

**٣٨ · لا تُدخِل `cd` في أمرٍ مركَّب.** الأوامرُ التالية تنكسر.
